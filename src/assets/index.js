const contactIcon = require('./icons/contact.png')
const chartIcon = require('./icons/chart.png')
const noData = require('./images/nodata.png')
const addContact = require('./images/people.png')

export const IMAGE = {
  chartIcon,
  contactIcon,
  noData,
  addContact,
}
